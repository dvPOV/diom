import {defineType, defineField} from 'sanity'

export default defineType({
  name: 'localeString',
  title: 'Localized string',
  type: 'object',
  fields: [
    defineField({name: 'en', type: 'string', title: 'English'}),
    defineField({name: 'ru', type: 'string', title: 'Русский'}),
    defineField({name: 'ka', type: 'string', title: 'ქართული'}),
  ],
})

import {defineType, defineField} from 'sanity'

export default defineType({
  name: 'localeText',
  title: 'Localized text',
  type: 'object',
  fields: [
    defineField({name: 'en', type: 'text', title: 'English'}),
    defineField({name: 'ru', type: 'text', title: 'Русский'}),
    defineField({name: 'ka', type: 'text', title: 'ქართული'}),
  ],
})

import {defineField, defineType} from 'sanity'
import localeString from './types/localeString'
import localeText from './types/localeText'

export default defineType({
  name: 'siteSettings',
  title: 'Site Settings (singleton)',
  type: 'document',
  fields: [
    // Base
    defineField({name: 'title', title: 'Project Title', type: 'string', validation: (Rule)=>Rule.required()}),
    defineField({name: 'instagramUrl', title: 'Instagram URL', type: 'url'}),
    defineField({name: 'logo', title: 'Logo (image)', type: 'image', options:{hotspot:true}}),

    // Navigation labels (i18n)
    defineField({
      name: 'nav',
      title: 'Navigation',
      type: 'object',
      fields: [
        defineField({name: 'services', type: 'localeString', title: 'Services'}),
        defineField({name: 'work', type: 'localeString', title: 'Portfolio'}),
        defineField({name: 'pricing', type: 'localeString', title: 'Pricing'}),
        defineField({name: 'location', type: 'localeString', title: 'Location'}),
        defineField({name: 'contact', type: 'localeString', title: 'Contacts'}),
        defineField({name: 'book', type: 'localeString', title: 'Book CTA'}),
      ],
    }),

    // Section titles (i18n)
    defineField({
      name: 'sectionTitles',
      title: 'Section Titles',
      type: 'object',
      fields: [
        defineField({name: 'services', type: 'localeString', title: 'Title: Services'}),
        defineField({name: 'work', type: 'localeString', title: 'Title: Portfolio'}),
        defineField({name: 'pricing', type: 'localeString', title: 'Title: Pricing'}),
        defineField({name: 'location', type: 'localeString', title: 'Title: Location'}),
        defineField({name: 'contact', type: 'localeString', title: 'Title: Contacts'}),
      ],
    }),

    // Global short texts (i18n)
    defineField({name: 'portfolioIntro', title: 'Portfolio intro', type: 'localeText'}),
    defineField({name: 'pricingNote', title: 'Pricing note', type: 'localeText'}),
    defineField({name: 'address', title: 'Address', type: 'localeString'}),

    // Contact form UI (i18n)
    defineField({
      name: 'ui',
      title: 'UI Texts',
      type: 'object',
      fields: [
        defineField({
          name: 'formLabels',
          title: 'Form labels',
          type: 'object',
          fields: [
            defineField({name: 'name', type: 'localeString', title: 'Name label'}),
            defineField({name: 'phone', type: 'localeString', title: 'Phone label'}),
            defineField({name: 'interest', type: 'localeString', title: 'Interest label'}),
            defineField({name: 'message', type: 'localeString', title: 'Message label'}),
            defineField({name: 'send', type: 'localeString', title: 'Send button'}),
          ],
        }),
        defineField({
          name: 'formPlaceholders',
          title: 'Form placeholders',
          type: 'object',
          fields: [
            defineField({name: 'name', type: 'localeString', title: 'Name placeholder'}),
            defineField({name: 'phone', type: 'localeString', title: 'Phone placeholder'}),
            defineField({name: 'message', type: 'localeString', title: 'Message placeholder'}),
          ],
        }),
      ],
    }),
  ],
})

import {defineField, defineType} from 'sanity'
import localeString from './types/localeString'

export default defineType({
  name: 'service',
  title: 'Service',
  type: 'document',
  fields: [
    defineField({name: 'title', type: 'localeString', title: 'Title', validation:(Rule)=>Rule.required()}),
    defineField({
      name: 'bullets',
      type: 'array',
      title: 'Bullets',
      of: [{type:'localeString'}],
    }),
  ],
})

import {defineField, defineType} from 'sanity'
import localeString from './types/localeString'
import localeText from './types/localeText'

export default defineType({
  name: 'vocalOffer',
  title: 'Vocal Offer',
  type: 'document',
  fields: [
    defineField({name: 'title', type: 'localeString', title: 'Title', validation:(Rule)=>Rule.required()}),
    defineField({name: 'paragraph', type: 'localeText', title: 'Paragraph'}),
    defineField({name: 'bullets', type: 'array', title: 'Bullets', of: [{type:'localeString'}]}),
    defineField({name: 'cta1', type: 'localeString', title: 'CTA 1'}),
    defineField({name: 'cta2', type: 'localeString', title: 'CTA 2'}),
    defineField({name: 'priceNote', type: 'localeString', title: 'Price Note'}),
    defineField({name: 'instagramId', type: 'string', title: 'Instagram Post ID', validation:(Rule)=>Rule.required()}),
  ],
})

import {defineField, defineType} from 'sanity'
import localeString from './types/localeString'

export default defineType({
  name: 'portfolioitem',
  title: 'Portfolio Item',
  type: 'document',
  fields: [
    defineField({name: 'title', type: 'localeString', title: 'Title', validation:(Rule)=>Rule.required()}),
    defineField({
      name: 'kind',
      type: 'string',
      title: 'Kind',
      options: {list: ['youtube','instagram']},
    }),
    defineField({name: 'videoId', type: 'string', title: 'Video/Short ID', validation:(Rule)=>Rule.required()}),
    defineField({name: 'ratio', type: 'string', title: 'Ratio', options: {list: ['16:9','3:4']}}),
  ],
})

import {defineField, defineType} from 'sanity'
import localeString from './types/localeString'

export default defineType({
  name: 'pricingCard',
  title: 'Pricing Card',
  type: 'document',
  fields: [
    defineField({name: 'title', type: 'localeString', title: 'Title', validation:(Rule)=>Rule.required()}),
    defineField({name: 'price', type: 'string', title: 'Price (fallback, shown if i18n is empty)'}),
    defineField({name: 'price_i18n', type: 'localeString', title: 'Price (i18n)'}),
    defineField({name: 'features', type: 'array', title: 'Features', of: [{type:'localeString'}]}),
  ],
})

import {defineField, defineType} from 'sanity'

export default defineType({
  name: 'locationImage',
  title: 'Location Image',
  type: 'document',
  fields: [
    defineField({name: 'photo', type: 'image', title: 'Photo', options:{hotspot:true}, validation:(Rule)=>Rule.required()}),
  ],
})

import siteSettings from './siteSettings'
import hero from './hero'
import service from './service'
import vocalOffer from './vocalOffer'
import portfolioItem from './portfolioItem'
import pricingCard from './pricingCard'
import locationImage from './locationImage'
import localeString from './types/localeString'
import localeText from './types/localeText'

export const schemaTypes = [
  // primitives
  localeString, localeText,
  // documents
  siteSettings, hero, service, vocalOffer, portfolioItem, pricingCard, locationImage,
]
