diff --git a/cms/schemas/types/localeString.ts b/cms/schemas/types/localeString.ts
new file mode 100644
index 0000000..3c8a1ff
--- /dev/null
+++ b/cms/schemas/types/localeString.ts
@@ -0,0 +1,13 @@
+import {defineType, defineField} from 'sanity'
+
+export default defineType({
+  name: 'localeString',
+  title: 'Localized string',
+  type: 'object',
+  fields: [
+    defineField({name: 'en', type: 'string', title: 'English'}),
+    defineField({name: 'ru', type: 'string', title: 'Русский'}),
+    defineField({name: 'ka', type: 'string', title: 'ქართული'}),
+  ],
+})
+
diff --git a/cms/schemas/types/localeText.ts b/cms/schemas/types/localeText.ts
new file mode 100644
index 0000000..0dcab77
--- /dev/null
+++ b/cms/schemas/types/localeText.ts
@@ -0,0 +1,13 @@
+import {defineType, defineField} from 'sanity'
+
+export default defineType({
+  name: 'localeText',
+  title: 'Localized text',
+  type: 'object',
+  fields: [
+    defineField({name: 'en', type: 'text', title: 'English'}),
+    defineField({name: 'ru', type: 'text', title: 'Русский'}),
+    defineField({name: 'ka', type: 'text', title: 'ქართული'}),
+  ],
+})
+
